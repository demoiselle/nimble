name = "Demoiselle Test Console Template"
description = "Demplate Used For Testing Reading template.conf"
vars {
    packageName { 
        label = "Package Name"
        defaultValue = "br.gov.frameworkdemoiselle.sample"
    }
	pojo { 
        label = "Entity Name"
        component {
            type = "comboBox"
            editable = true
        }
    }
}

/*
passa 0 argumentos, perguntar pelos obrigatorios (1, 3)
passa 1 argumentos, perguntar pelo 3o
passa 2 argumentos, entende que o 1o é packageName, o 2o é projectName, vai perguntar pelo 3o 
passa 2 argumentos, entende os 2 primeiros e vai embora
passa 3 argumentos, entende os 3 e vai embora
*/